function setup() {
    // put setup code here
    createCanvas(windowWidth, windowHeight);
    
  }
  
  function draw() {
    // put drawing code here
  
    // line(0, 0, 99, 99);
    // line(0, 0, windowWidth, windowHeight);
    // line(random(windowWidth), random(windowHeight), random(windowWidth), random(windowHeight));
    // line(random(100), random(100), random(100), random(100));
    line(random(500), random(500), random(500), random(500));
    line(random(500, 1000), random(500), random(500, 1000), random(500));
    line(random(1000, 1500), random(500), random(1000, 1500), random(500));
  }
  
  